<ul>
  <li><%= @product.id %></li>
  <li><%= @product.name %></li>
</ul>

<ul>
  <%@product.images.each do |img| %>
    <li>
      <%= image_tag url_for(img) %>
    </li>
    <li>
      <%= image_tag img.variant(resize: "100x100") %>
    </li>
  <% end %>
</ul>
