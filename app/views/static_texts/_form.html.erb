<%= form_with(model: static_text, local: true) do |form| %>
  <% if static_text.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(static_text.errors.count, "error") %> prohibited this static_text from being saved:</h2>

      <ul>
        <% static_text.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
